import React from 'react'
import axios from 'axios'
import {
    base
} from '../../components/const'

const API = base

export default class PlayersCharacter extends React.Component{
    constructor(props){
        super(props)
        this.state = {
            data: []
        }
    }

    render(){

        return(
            <div>
                
            </div>
        )
    }
}